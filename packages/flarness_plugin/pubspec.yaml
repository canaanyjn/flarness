name: flarness_plugin
description: Debug-only Flutter integration package for Flarness UI automation.
version: 0.1.0
homepage: https://github.com/canaanyjn/flarness

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
